/**
 * Shared backgrounds selected by sequence metadata.
 * A card may still provide a custom image, but that is an explicit override.
 */
export const SEQUENCE_BACKGROUNDS: Partial<Record<number, string>> = {
  5: '/sequence-back/sequence-5.png',
}
